apiVersion: v1
kind: Service
metadata:
  labels:
    app: dotnetapp
    tier: frontend
  name: dotnetapp
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
  selector:
    app: dotnetapp
